println "Hello, System.out?"

logger.info "Hello, logger!"

// Example of using the 'apply' function
apply { println "Inside my 'apply' closure" }

defaultTasks 'hello'

// Add a trivial task named 'hello'
task ('hello').doLast {
	println "Hello World!"
}

group 'com.jimtough'
